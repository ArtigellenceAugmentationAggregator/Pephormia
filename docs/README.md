# PEPHORMIA — static site (deploy from /docs)
Plain HTML, no build step.
Update: drag this whole `docs` folder into the repo root -> Commit. Pages already serves /docs.
Edit look in docs/assets/styles.css. Subscribe form: docs/index.html, search "WIRE YOUR LIST".
