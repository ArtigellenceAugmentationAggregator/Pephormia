PEPHORMIA — COMPLETE SITE FOLDER (build-verified)
==================================================
This is the whole website. It BUILDS CLEANLY (verified with `npm run build` — 8 pages).

WHAT'S INSIDE
  src/pages/        index (home), shop, invest, learn, library, blog/
  src/layouts/      BaseLayout.astro  (nav incl. Investors + footer)
  src/components/   EvidenceBox.astro (four-tier graded evidence)
  src/content/blog/ 2 starter posts (add more .md files here)
  src/styles/       tokens.css (Lab Bright)
  public/           CNAME (pephormia.com), favicon, robots
  .github/workflows/deploy.yml  (auto-deploy on push to main)

PUT IT LIVE (replaces your repo's site folder)
  1. In your GitHub repo, delete the old  pephormia-site  folder.
  2. Upload THIS  pephormia-site  folder in its place (same name, repo root).
  3. Commit -> push to main.  Actions rebuilds, ~2 min, site updates.
  4. Hard-refresh pephormia.com (Ctrl+Shift+R).

  IMPORTANT: your site lives INSIDE the pephormia-site/ folder in your repo.
  Keep that structure. Do NOT drop files at the repo root.

PREVIEW LOCALLY
  npm install
  npm run dev   ->  http://localhost:4321

NOTE: home/learn/library/blog are fresh versions. Your previous wording is still in
your GitHub commit history if you want any of it back. Shop + Investors are the real pages.
The GHK-Cu Decoded product is set to "Launching soon" until its citation pass is re-rendered.
